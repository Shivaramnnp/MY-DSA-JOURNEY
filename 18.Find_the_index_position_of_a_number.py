#Find the index position of a number.
def IndexPostion(number, target):
    for num in number:
     return numbers.index(target)
numbers = [10,20,30,40,50]
target = 20
print(IndexPostion(numbers,target))